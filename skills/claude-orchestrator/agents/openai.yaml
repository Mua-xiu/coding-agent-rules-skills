interface:
  display_name: "Claude Orchestrator"
  short_description: "Delegate local Claude Code work."
  default_prompt: "Use $claude-orchestrator to analyze the current task, route bounded work to the matching architect, implementer, or mechanic profile only when that profile exists and is healthy, fall back to Codex when delegation is unavailable, use stdout handoff by default, and review real artifacts before accepting."

policy:
  allow_implicit_invocation: false
