interface:
  display_name: "Claude Orchestrator"
  short_description: "Delegate local Claude Code work."
  default_prompt: "Use $claude-orchestrator to analyze the current task, route it to architect, implementer, or mechanic by fixed profile rules, switch the Claude profile when needed, require a handoff, and review the changed files before accepting."

policy:
  allow_implicit_invocation: false
