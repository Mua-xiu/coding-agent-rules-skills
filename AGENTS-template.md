# 协作规则

## 回复与思考语言

- 使用中文回答用户。
- 分析问题、说明思路和解释改动原因时，优先使用中文表达，方便后续维护和复盘。

## 方案选择原则

- 默认优先推荐改动最小的方案，避免无必要的大范围重构。
- 推荐方案时需要说明思路和原因，让后续维护者能理解为什么这样改。
- “改动最小”必须建立在有效、可靠、无明显隐患的前提下；如果最小改动存在较严重隐患，应明确说明风险，并推荐更稳妥、可落地的有效方案。
- 不为了追求最小改动而保留会导致明显 bug、性能问题、安全风险或后续维护困难的实现。

## 任务边界与确认

- 收到任务并完成初步分析后，如果存在影响实现方向、改动范围或验收标准的不确定点，应先向用户询问确认，不要依靠发散性推测自行创造需求。
- 处理任务时应始终围绕用户明确给出的范围执行；对于任务中不明确的部分、提示词遗漏的重大漏洞或可能涉及危险操作的内容，应先说明风险并询问是否需要补充修复。
- 不要盲目添加与当前任务无关的功能、创新设计或额外重构；只有在这些改动对完成任务确有必要时，才应提出原因并等待用户确认。
- 如果认为某处需要为未来预留扩展空间，应先说明预留的价值、成本和影响范围，并在得到用户同意后再添加相关设计或代码。

## 注释规则

- 修改代码时需要保持良好的中文注释习惯，让后续维护者能快速理解这段代码为什么存在、负责什么能力。
- 注释应简洁说明代码的具体作用、业务原因或保留意图，避免只写“修改”“暂时处理”等无法复盘的信息。
- 编写或修改函数、方法、类、组件和模块时，应主动添加简短中文注释说明职责、关键参数、返回结果或副作用；关键业务函数和关键类不应缺少说明性注释。
- 对关键变量、常量、配置项、枚举、映射表、阈值和状态标记，应添加简短中文注释说明含义、取值来源或业务用途。
- 前端 HTML、Vue template、JSX 等结构中，主要区域之间应添加区块名称注释，例如头部、筛选区、列表区、弹窗区、操作区等，方便快速定位页面结构。
- 对隐藏、停用、临时保留旧逻辑、绕开已停用服务、兼容历史逻辑、修复旧模块问题等改动，应添加简短中文注释说明原因和恢复意图。
- 正常编写新模块或新增常规代码时，也应在关键方法、关键判断或不够直观的业务逻辑处添加简略中文注释，说明这块代码的作用。
- 不要为了注释而给一眼能看懂的普通赋值、简单模板结构或重复代码添加冗余说明。
- 只有在为了修复 bug、处理旧模块问题、隐藏/停用历史能力或调整既有逻辑时，注释中才需要添加日期。
- 正常编写新模块代码时，注释不需要添加日期。

## 旧代码处理偏好

- 对暂时不用但后续可能恢复的旧逻辑，优先注释保留并说明原因，不直接删除。
- 如果不确定旧代码逻辑是否需要删除，应先询问用户，等待明确回答后再决定删除还是注释保留。
- 如果确认代码已经彻底无用，且删除不会影响恢复和排查，可以在说明原因后删除；不应为了形式保留无意义代码。
